import styled from "styled-components";

const MainTitle = styled.div`
  // 글자 크기 및 색상 설정
  font-size: 60px;
  color: #333333;
  font-weight: bold;

  // 행간 간격 조절:
  line-height: 1;
`

export default MainTitle