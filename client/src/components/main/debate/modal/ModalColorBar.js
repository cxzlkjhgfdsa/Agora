import styled from "styled-components";

const StyledModalColorBar = styled.div`
  width: 8px;
  height: 70%;
  background-color: #FFFFFF;
`;

function ModalColorBar() {
  return (
    <StyledModalColorBar />
  );
}

export default ModalColorBar;